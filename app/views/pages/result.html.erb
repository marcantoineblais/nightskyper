<%= link_to "Back to Search page", search_path %>

<h1>result page</h1>

<%= render 'overview', today: @meteo_prediction.first %>

<%= render 'prediction-card', meteo_prediction: @meteo_prediction %>

<div class="d-flex">
  <div class="reviews flex-grow-1">
    <%= render 'review-card' %>
  </div>
  <div>
  <%= link_to 'Add a review', new_marker_review_path(@marker) %>
  </div>
</div>
